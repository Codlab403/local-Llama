[project]
name = "llamainde-backend"
version = "0.1.0"
description = "Local-first RAG backend prototype"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 88
